{
  "name": "birdie-test",
  "version": "1.0.0",
  "description": "We need a root package.json beacuse it is the only way supported by heroku.",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd backend && npm install && npm run build",
    "start": "cd backend && ./node_modules/.bin/pm2-runtime start ./dist/index.js"
  },
  "author": "",
  "license": "ISC"
}
