{
  "name": "birdie-test",
  "version": "1.0.0",
  "description": "We need a root package.json beacuse it is the only way supported by heroku.",
  "main": "index.js",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && npm start"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "14.x"
  }
}
